[build]
buildCommand = "npm ci && npx prisma generate"

[deploy]
startCommand = "npm run build && npx prisma migrate deploy && node dist/main.js"
healthcheckPath = "/api"
restartPolicyType = "on_failure"